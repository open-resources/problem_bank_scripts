# Required
version: 2

# Image to use
build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"

# Configuration
python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py
